# TODO — openon4net-runtime (Plane 1)

> فایل تصمیم‌گیری، نه صف اجرا. من (Claude) هیچ‌کدوم از این ردیف‌ها رو خودم
> شروع نمی‌کنم — منتظر می‌مونم بگی کدوم رو انجام بدم (تک‌تک یا گروهی).
> بعد از هر تسک، وضعیتش اینجا و خلاصه‌اش در `docs/spect/DONE.md` به‌روز می‌شه.
>
> **محدوده:** همه‌ی ردیف‌ها (مگر خلافش نوشته شده) فقط داخل
> `apps/openon4net-runtime/` اجرا می‌شن، با کامیت جفتی (submodule + bump
> pointer در روت). ردیف‌هایی که به فایل ریشه‌ی مونوریپو نیاز دارن
> (`pnpm-workspace.yaml` و غیره) صراحتاً علامت‌گذاری شدن — که برای این Plane
> عملاً پیش نمیاد چون از قبل ثبت شده.
>
> این فایل خواهر `TODO-openon4net-memory.md` است، همون فرمت.

---

## بخش A — کم‌ریسک، خلأهای مشخص و مجزا (هر کدوم مستقل قابل انجامه)

| #      | تسک                                                                              | یادداشت                                                                     |    وضعیت     |
| ------ | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | :----------: |
| RT-001 | Audit log viewer (`GET /v1/audit` + صفحه داشبورد)                                | ✅ انجام شد — جزئیات در `DONE.md`                                           |      ✅      |
| RT-002 | Workspace CRUD (`POST`/`GET /v1/workspaces`)                                     | ✅ انجام شد — جزئیات در `DONE.md`                                           |      ✅      |
| RT-003 | ساخت/حذف نقش سفارشی (`POST`/`DELETE /v1/roles`)                                  | ✅ انجام شد — جزئیات در `DONE.md`                                           |      ✅      |
| RT-004 | تغییر نقش کاربر بعد از ساخت + غیرفعال/حذف کاربر (`PATCH`/`DELETE /v1/users/:id`) | ✅ انجام شد — جزئیات در `DONE.md`                                           |      ✅      |
| RT-005 | تست واقعی ارسال تلگرام                                                           | کد کامله، فقط نیاز به یک bot token واقعی از طرف شما (بلاک شده، نه فنی)      | ⏸️ منتظر شما |
| RT-006 | یک connector دیگه علاوه بر تلگرام (مثلاً email یا generic webhook)               | ✅ انجام شد — generic webhook connector با محافظت SSRF، جزئیات در `DONE.md` |      ✅      |

## بخش B — دامنه‌ی متوسط، قابلیت جدید واقعی (هنوز کاملاً داخل Runtime)

| #      | تسک                                                                                              | یادداشت                                                                                                                                                                                                                                                                                                                                                                | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-007 | اجرای زمان‌بندی‌شده‌ی Agent (`agents.schedule`)                                                  | ✅ انجام شد — scheduler درون‌فرآیندی 30 ثانیه‌ای، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                  |  ✅   |
| RT-008 | ABAC Policy Layer (§6 سند `10-rbac-and-policy.md`) — جدول `policies` + شرایط cost/layer/tag/time | ✅ انجام شد (subset: `cost_gt_cents`, `outside_hours`) — condition typeهای بیشتر (layer/tag/environment) هنوز نیست، جزئیات در `DONE.md`                                                                                                                                                                                                                                |  ✅   |
| RT-009 | نمایش بودجه/هزینه در داشبورد                                                                     | ✅ انجام شد — `BudgetBar` در صفحه Agents، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                          |  ✅   |
| RT-010 | نمایش وضعیت rate limit                                                                           | ✅ انجام شد — badge در صفحه چت، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                    |  ✅   |
| RT-021 | صفحه Chat مطابق UI مرجع (۳ پنل: Control/Chat/Workspace) + پشتیبانی RTL/LTR بر اساس زبان انتخابی  | ✅ انجام شد — ۳ پنل دائمی (نه توگل): Control (سوییچ Agent + `+` ساخت Agent برای admin + `+ New session` + لیست sessionها)، Chat (وسط، بدون تغییر)، Workspace (فایل‌ها + refresh). زیر RTL کل ردیف با `row-reverse` mirror می‌شه. **محدودیت تست**: بدون ابزار مرورگر، تعامل واقعی کلیک-به-کلیک تست نشد — جزئیات در `DONE.md`                                            |  ✅   |
| RT-022 | Session management در UI (هر Agent چند session + `+ New session` + Recent)                       | ✅ انجام شد — `GET/POST /v1/agents/:id/conversations` + rename/archive + fetch-by-id، پنل «Sessions» توگل‌شونده در صفحه‌ی چت. جزئیات در `DONE.md`                                                                                                                                                                                                                      |  ✅   |
| RT-023 | Agent picker + دکمه `+` کنار لیست Agentها برای ساخت Agent (modal)                                | ✅ انجام شد — دکمه `+ New agent` فقط برای `session.role === 'admin'` نمایش داده می‌شه؛ فرم ساخت Agent حالا به‌جای انتخاب دستی workspace، یک workspace اختصاصی (۱:۱) خودکار می‌سازه (`{name} Workspace`). picker دستی workspace حذف شد (کد مرده). تست HTTP واقعی end-to-end. جزئیات در `DONE.md`                                                                        |  ✅   |
| RT-024 | Agent Access (grant/revoke) — کاربر به یک یا چند Agent دسترسی داشته باشد                         | ✅ انجام شد — `agent_access_bindings` + enforce روی list/get/chat/tools/memory (نه فقط visibility)، جزئیات در `DONE.md`                                                                                                                                                                                                                                                |  ✅   |
| RT-025 | Workspace فایل‌محور per-agent (نمایش پنل Files و جداسازی workspaceها)                            | ✅ انجام شد — `POST/GET/DELETE /v1/agents/:agentId/files` (private، presigned URL از MinIO، `requireAgentAccessible` جداسازی رو enforce می‌کنه) + پنل «Files» توگل‌شونده در صفحه‌ی چت. یک باگ امنیتی واقعی توی زیرساخت مشترک RT-030 حین کار پیدا/فیکس شد (پایین ببینید). جزئیات در `DONE.md`                                                                           |  ✅   |
| RT-026 | Skill grants per-agent (admin اجازه Skill/Tool به Agent بدهد)                                    | ✅ انجام شد — `agent_skill_grants` + `SkillGrantService` + UI (`/skills` صفحه) + audit (`skill-grant`/`skill-revoke`)، جزئیات در `DONE.md`                                                                                                                                                                                                                             |  ✅   |
| RT-027 | نصب ابزارها/پلاگین‌ها از Marketplace + ZIP + حالت دستی (self-hosted)                             | ✅ کامل شد — مسیر Marketplace از قبل (RT-035/RT-036)؛ حالا `POST /v1/plugins/upload` (ZIP دستی، `manifest.json` از ریشه‌ی zip استخراج می‌شه، بدون هیچ extract دیگه‌ای روی دیسک) + بخش «Self-hosted plugins» در صفحه‌ی Marketplace. جزئیات در `DONE.md`                                                                                                                 |  ✅   |
| RT-028 | Feature gating برای Programmer Agent/Development skills بر اساس لایسنس AI Gateway                | ✅ انجام شد — `license-service.ts`'s `hasFeature()` (پیش‌فرض false، برخلاف `isActivated()`)؛ گیت روی create/update Agent (role=programmer) در `routes/agents.ts` و create Plugin (category=devops) در `routes/local-plugins.ts`؛ خطای `FeatureNotAvailableError` (کد جدید `FEATURE_NOT_AVAILABLE`) + audit روی رد شدن. تست HTTP واقعی: ۴۰۲ درست. جزئیات در `DONE.md`   |  ✅   |
| RT-029 | Auto-migrate روی startup (اولین بالا آمدن سیستم + upgradeها)                                     | ✅ انجام شد — `schema_migrations` + advisory lock + fail-fast + `DB_AUTO_MIGRATE=false` + `pnpm run migrate` دستی، جزئیات در `DONE.md`                                                                                                                                                                                                                                 |  ✅   |
| RT-030 | Settings: شخصی‌سازی لوگو (Branding)                                                              | ✅ انجام شد — `POST /v1/organization/branding` (multipart، MinIO/S3 واقعی، `branding:update` + audit) + UI در Settings + نمایش در Sidebar. **عمداً نه**: صفحه‌ی login (قبل از auth، سازمان هنوز شناخته‌شده نیست). جزئیات در `DONE.md`                                                                                                                                  |  ✅   |
| RT-031 | Context Contract + Prompt Builder (identity/task/workspace/memory/tools/permissions/language)    | ✅ انجام شد — `services/context-builder.ts` (۸ لایه‌ی سند به‌جز `trace` که همیشه بوده) + `services/prompt-builder.ts` (فشرده‌سازی به یک system message). یک باگ واقعی توی زیرساخت مشترک تست‌ها پیدا/فیکس شد (پایین ببینید). جزئیات در `DONE.md`                                                                                                                        |  ✅   |
| RT-032 | Skill Engine Core (`skills` CRUD + Skill Executor) — فاز ۲ roadmap                               | ✅ انجام شد — `SkillService`+`routes/skills.ts` (CRUD)، `tool-dispatcher.ts`+`skill-executor.ts` (اجرای واقعی روی webhook-send/telegram-send). v1: فقط `steps[].type: 'tool'` و `trigger.type: 'manual'` — `query`/`prompt` step types و scheduled/event trigger موکول به بعد. جزئیات در `DONE.md`                                                                     |  ✅   |
| RT-033 | Auto-Skill Detection + Proposal Review — فاز ۲ roadmap                                           | ✅ انجام شد — `skill-pattern-detector.ts` (Frequency+Similarity روی `audit_logs`، ۲ از ۴ شرط سند — Duration/Complexity سیگنال ندارن) + `skill_proposals` + `routes/skill-proposals.ts` (approve/reject) + صفحه `/skill-proposals`. جزئیات در `DONE.md`                                                                                                                 |  ✅   |
| RT-034 | Activation Client سمت Runtime (T-CP-007) — فاز ۲ تکمیلی                                          | ✅ انجام شد — `activation-client.ts` (`checkIn()`، best-effort)، `activation-state.ts` (`ActivationState.isActivated()` — بدون پیکربندی همیشه `true`، grace window ۲۴ ساعته بعد از پیکربندی)، `activation-scheduler.ts` (poller ساعتی). ۴ تست vitest با `http.createServer` محلی. جزئیات در `DONE.md`                                                                  |  ✅   |
| RT-035 | Marketplace install-proxy routes (Skills + Plugins) — فاز ۲ تکمیلی                               | ✅ انجام شد — `services/marketplace-client.ts` + `routes/marketplace.ts`: `GET /v1/marketplace/{plugins,skills}`، `POST .../install` (activation-gated برای هر دو، رایگان و پولی یکسان — ببینید ADR-012)، نصب Skill کپی `definition` را در یک ردیف local `skills` می‌ریزد. ۵ تست vitest. جزئیات در `DONE.md`                                                           |  ✅   |
| RT-036 | Plugin Settings UI (schema-driven config form) — فاز ۲ تکمیلی                                    | ✅ انجام شد — `PATCH /v1/marketplace/installs/:installId/config` (پروکسی به Marketplace)، صفحه `web/app/marketplace/page.tsx` (لیست Skill/Plugin + Install + فرم تنظیمات بر اساس `manifest.configSchema`). فرم فقط write (بدون GET تک‌نصب، مقدار قبلی pre-populate نمی‌شود). جزئیات در `DONE.md`                                                                       |  ✅   |
| RT-037 | Plugin SDK + CLI (`@o2n/plugin-sdk`, `create-o2n-plugin`) — فاز ۲ تکمیلی                         | ✅ انجام شد — `packages/plugin-sdk` (`createPlugin`/`defineTool`/`defineAction`، فقط type/manifest-building، بدون sandboxed execution) + `packages/create-o2n-plugin` (CLI اسکلت‌ساز، `pnpm create o2n-plugin <name>`). ۳ تست vitest برای CLI. جزئیات در `DONE.md`                                                                                                     |  ✅   |
| RT-038 | Organization CRUD واقعی — فاز ۳ (هفته ۲۵-۲۶)                                                     | ✅ انجام شد — `OrgService.getById`/`update` + `GET`/`PATCH /v1/organization` (بدون :id در URL، فقط سازمان همون session)؛ `plan`/`status` عمداً غیرقابل‌ویرایش (کار Control-Plane/activation است). بخش Organization در `web/app/settings/page.tsx`. جزئیات در `DONE.md`                                                                                                 |  ✅   |
| RT-039 | Workspace CRUD تکمیل (update + archive) — فاز ۳ (هفته ۲۵-۲۶)                                     | ✅ انجام شد — ستون `workspaces.status` (migration 0016)؛ `PATCH`/`POST .../archive`. حذف واقعی عمداً جایگزین نشد چون `agents.workspace_id` `ON DELETE CASCADE` داره — آرشیو، نه delete. ساخت agent در workspace آرشیوشده رد می‌شه. UI ادیت/آرشیو در `/workspaces`. جزئیات در `DONE.md`                                                                                 |  ✅   |
| RT-040 | تکمیل RBAC: تخصیص نقش سفارشی + binding به workspace انتخابی — فاز ۳ (هفته ۲۷-۲۸)                 | ✅ انجام شد — `UserCreateSchema`/`UserUpdateSchema`'s `role` از enum ۴تایی به هر نام نقش موجود در سازمان باز شد (DB از قبل همینو اجازه می‌داد، فقط Zod محدود بود)؛ `workspaceId` اختیاری اضافه شد. UI: `/users`'s role select از `GET /v1/roles` می‌خونه. جزئیات در `DONE.md`                                                                                          |  ✅   |
| RT-041 | سیستم دعوت با ایمیل (Invitation) — فاز ۳ (هفته ۲۵-۲۶)                                            | ✅ انجام شد — جدول `invitations` (migration 0017)، `InvitationService` (همون الگوی hashed-token مگ‌لینک، TTL هفت‌روزه)، `POST/GET /v1/invitations`، `POST /v1/auth/invitations/:token/accept` (public)، صفحه `/accept-invite`. ایمیل با همون `nodemailer` مگ‌لینک. جزئیات در `DONE.md`                                                                                 |  ✅   |
| RT-042 | تعمیم صف تأیید (Approval Queue) — فاز ۳ (هفته ۲۷-۲۸)                                             | ✅ انجام شد — `ApprovalService.create()` عمومی (نه فقط از داخل chat)، `expireStale()` + `approval-expiry-scheduler.ts` (sweep هر ۵ دقیقه)، صفحه `/approvals`. تریگر فعلی هنوز فقط chat-cost/policy است — این batch زیرساخت رو عمومی کرد، نه سطح جدید trigger. جزئیات در `DONE.md`                                                                                      |  ✅   |
| RT-043 | بودجه سازمان/workspace با جدول `wallets` — فاز ۳ (هفته ۲۷-۲۸)                                    | ✅ انجام شد — `WalletService` (credit/debit روی `wallets` که تا الان schema-only بود)، `GET /v1/wallet` + `POST /v1/wallet/credit`، budget gate سوم در `chat-service.ts` (اگه wallet ساخته شده باشه). اختیاری/opt-in — سازمان بدون wallet یعنی بدون سقف. جزئیات در `DONE.md`                                                                                           |  ✅   |
| RT-044 | Role Catalog — فاز ۳ (هفته ۲۹-۳۰)                                                                | ✅ انجام شد — `AGENT_ROLE_CATALOG` در `packages/shared`، `agents.role` همچنان `VARCHAR` آزاد (بدون migration). UI: `<select>` + «Other…» در `/agents`. جزئیات در `DONE.md`                                                                                                                                                                                             |  ✅   |
| RT-045 | Agent Hierarchy + Team assignment — فاز ۳ (هفته ۲۹-۳۰ و ۳۱-۳۲)                                   | ✅ انجام شد — `assertNoReportsToCycle` (رد self-reference/چرخه)، `listReports`/`listTeam` (زیردرخت transitive، بدون جدول جدید)، org-chart تو-رفته در UI. یک باگ واقعی پیدا/فیکس شد: `update()` چرخه رو چک می‌کرد ولی `reports_to` رو ذخیره نمی‌کرد. جزئیات در `DONE.md`                                                                                                |  ✅   |
| RT-046 | Agent KPI definition — فاز ۳ (هفته ۲۹-۳۰)                                                        | ✅ انجام شد — `KpiDefinitionSchema` + `updateKpis` (merge روی `kpi_config` JSONB) + پنل UI. فقط target/current ادمین‌تنظیم، بدون محاسبه‌ی خودکار (Outcome Engine مال فاز ۴). جزئیات در `DONE.md`                                                                                                                                                                       |  ✅   |
| RT-047 | Agent-to-Agent communication — فاز ۳ (هفته ۳۱-۳۲)                                                | ✅ انجام شد — migration `0018_agent_messages.sql`، `AgentMessageService` + `agent-message-scheduler.ts` (poll ۳۰ ثانیه‌ای، async/fire-and-forget). تحویل واقعی از طریق scheduler تست نشده (نیاز LLM provider واقعی). جزئیات در `DONE.md`                                                                                                                               |  ✅   |
| RT-048 | Workflow Engine (simple DAG) + Task delegation — فاز ۳ (هفته ۳۱-۳۲)                              | ✅ انجام شد — `agent`/`tool`/`human`/`parallel`/`condition` step types، migration `0019_workflows.sql`، `WorkflowExecutor` (استفاده‌ی مستقیم از `executeTool`/`ChatService`/`ApprovalService`). v1: بدون `loop`/`notification`/Visual Builder/scheduled trigger. صفحه `/workflows`. یک باگ واقعی دوم پیدا/فیکس شد: `logStep()`'s پارامتر Postgres. جزئیات در `DONE.md` |  ✅   |
| RT-049 | Skill versioning + fork — فاز ۲ (هفته ۱۷-۱۸)                                                     | ✅ انجام شد — `update()` روی تغییر definition نسخه رو patch-bump می‌کنه، `fork()` + `POST /v1/skills/:id/fork`. جزئیات در `DONE.md`                                                                                                                                                                                                                                    |  ✅   |
| RT-050 | Skill validation module — فاز ۲ (هفته ۱۹-۲۰)                                                     | ✅ انجام شد — `skill-validator.ts` (شناسه‌ی تکراری، سقف step، SSRF guard وب‌هوک در زمان ذخیره). یک باگ واقعی پیدا/فیکس شد: پیام خطای اشتباه `ToolExecutionError`. جزئیات در `DONE.md`                                                                                                                                                                                  |  ✅   |
| RT-051 | Marketplace permission-diff consent wiring (سمت Runtime) — فاز ۲ (هفته ۲۱-۲۲)، همراه MKT-020     | ✅ انجام شد — `acknowledgePermissionDiff` تا انتهای UI وایر شد، permissions در discovery نشون داده می‌شه. جزئیات در `DONE.md`                                                                                                                                                                                                                                          |  ✅   |
| RT-052 | Marketplace analytics (downloads/ratings؛ سمت Runtime) — فاز ۲ (هفته ۲۳-۲۴)، همراه MKT-021       | ✅ انجام شد — proxy route ها + UI برای installCount/avgRating/rate. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                |  ✅   |
| RT-053 | Marketplace publisher dashboard (سمت Runtime) — فاز ۲ (هفته ۲۳-۲۴)، همراه MKT-022                | ✅ انجام شد — صفحه‌ی `/marketplace/publisher` + permission جدید `marketplace:publish`. یک gap واقعی پیدا/فیکس شد: `marketplace:read/install` توی seed پیش‌فرض admin نبودن. جزئیات در `DONE.md`                                                                                                                                                                         |  ✅   |
| RT-054 | Audit Log: export + retention — فاز ۳ (هفته ۲۷-۲۸)                                               | ✅ انجام شد — `GET /v1/audit/export`، `organizations.settings.auditRetentionDays` + `AUDIT_RETENTION_DAYS` + scheduler روزانه. جزئیات در `DONE.md`                                                                                                                                                                                                                     |  ✅   |
| RT-055 | Audit Log: tamper-evidence — فاز ۳ (هفته ۲۷-۲۸)                                                  | ✅ انجام شد — migration `0020_audit_chain.sql`، زنجیره‌ی هش SHA-256، `verifyChain()`، checkpoint هماهنگ با retention. محدودیت مستندشده: race احتمالی بدون transaction lock. جزئیات در `DONE.md`                                                                                                                                                                        |  ✅   |
| RT-056 | Human-in-the-Loop عمومی — فاز ۳ (هفته ۲۷-۲۸)                                                     | ✅ انجام شد — `PolicyCondition`'s نوع `action_type_in` + قلاب در `routes/tools.ts`'s دو route مستقیم. عمداً Workflow steps رو نگرفت. جزئیات در `DONE.md`                                                                                                                                                                                                               |  ✅   |
| RT-057 | Payment integration (Marketplace paid install debit) — فاز ۴ (هفته ۳۳-۳۴)، همراه MKT-023         | ✅ انجام شد — قبل از نصب قیمت پرسیده می‌شه، `WalletService.debit()` قبل از نصب صدا زده می‌شه (debit-قبل-از-نصب، بدون نیاز به rollback). پرداخت واقعی (Stripe) عمداً stub موند. جزئیات در `DONE.md`                                                                                                                                                                     |  ✅   |
| RT-058 | KPI computation engine + snapshot history — فاز ۴ (هفته ۳۵-۳۶)                                   | ✅ انجام شد — `metricType`/`windowDays` روی KPI definition، `agent_kpi_snapshots` + `kpi-computation-service.ts` + `kpi-snapshot-scheduler.ts` (روزانه). جزئیات در `DONE.md`                                                                                                                                                                                           |  ✅   |
| RT-059 | BI Dashboard — فاز ۴ (هفته ۳۵-۳۶)                                                                | ✅ انجام شد — صفحه‌ی `/outcomes` با sparkline دست‌ساز SVG، روت تجمیعی `kpi-outcomes`. جزئیات در `DONE.md`                                                                                                                                                                                                                                                              |  ✅   |
| RT-060 | Automated insights — فاز ۴ (هفته ۳۵-۳۶)                                                          | ✅ انجام شد — `insight-generator.ts`، هیوریستیک آستانه‌ای صریح (نه NLG). جزئیات در `DONE.md`                                                                                                                                                                                                                                                                           |  ✅   |
| RT-061 | Auto-reporting (daily/weekly) — فاز ۴ (هفته ۳۷-۳۸)                                               | ✅ انجام شد — opt-in، `report-scheduler.ts` (ایمیل فقط اگه SMTP باشه) + `GET /v1/reports/latest` on-demand. جزئیات در `DONE.md`                                                                                                                                                                                                                                        |  ✅   |
| RT-062 | Anomaly detection — فاز ۴ (هفته ۳۷-۳۸)                                                           | ✅ انجام شد — `anomaly-detector.ts`، Z-score، هیوریستیک آماری صریح (نه ML). جزئیات در `DONE.md`                                                                                                                                                                                                                                                                        |  ✅   |
| RT-063 | Predictive analytics — فاز ۴ (هفته ۳۷-۳۸)                                                        | ✅ انجام شد — `trend-predictor.ts`، رگرسیون خطی OLS، برون‌یابی صریح (نه پیش‌بینی واقعی). جزئیات در `DONE.md`                                                                                                                                                                                                                                                           |  ✅   |
| RT-064 | Natural language query on data — فاز ۴ (هفته ۳۷-۳۸)                                              | ✅ انجام شد — `nl-query-service.ts`، LLM فقط یک intent ثابت Zod-validated انتخاب می‌کنه (نه SQL آزاد). تست خودکار با provider جعلی؛ **حین نوشتن tutorial (RT-075) با LLM واقعی هم دستی تست شد** — جواب واقعی گرفت. جزئیات در `DONE.md`                                                                                                                                 |  ✅   |
| RT-065 | Inbound webhooks — فاز ۴ (هفته ۳۹-۴۰)                                                            | ✅ انجام شد — `webhook_endpoints` (token فقط hash ذخیره می‌شه)، `POST /v1/webhooks/:token` (public)، rate-limited، صفحه‌ی `/webhooks`. جزئیات در `DONE.md`                                                                                                                                                                                                             |  ✅   |
| RT-066 | Workflow triggers (scheduled + webhook) — فاز ۴ (هفته ۳۹-۴۰)                                     | ✅ انجام شد — ستون `workflows.trigger`، `workflow-trigger-scheduler.ts` (همون شکل interval `agents.schedule`). جزئیات در `DONE.md`                                                                                                                                                                                                                                     |  ✅   |
| RT-067 | Import/Export برای Skills + Workflows — فاز ۴ (هفته ۳۹-۴۰)                                       | ✅ انجام شد — `GET /v1/skills/:id/export` / `GET /v1/workflows/:id/export`؛ import یعنی همون create endpoint موجود. UI: دکمه‌ی Export + paste-JSON import. جزئیات در `DONE.md`                                                                                                                                                                                         |  ✅   |
| RT-068 | SSO سازمانی — OIDC per-org — فاز ۵ (هفته ۴۱-۴۲)                                                  | ✅ انجام شد — `sso_configs` + `auth/providers/oidc.ts` (discovery دستی، بدون کتابخونه). عمداً auto-provision نمی‌کنه. جزئیات در `DONE.md`                                                                                                                                                                                                                              |  ✅   |
| RT-069 | SSO سازمانی — SAML per-org — فاز ۵ (هفته ۴۱-۴۲)                                                  | ⚠️ انجام شد — `auth/providers/saml.ts` با `@node-saml/node-saml`. IdP واقعی امضادار end-to-end تست نشده (بلاک‌کلاس RT-005/RT-017). جزئیات در `DONE.md`                                                                                                                                                                                                                 |  ⚠️   |
| RT-070 | Health check سخت‌تر + Kubernetes manifests — فاز ۵ (هفته ۴۳-۴۴)                                  | ✅ انجام شد — `/health` واقعاً DB/Redis چک می‌کنه، `k8s/*.yaml`. کلاستر واقعی برای تست نداشتیم. جزئیات در `DONE.md`                                                                                                                                                                                                                                                    |  ✅   |
| RT-071 | Backup/restore system — فاز ۵ (هفته ۴۵-۴۶)                                                       | ✅ کامل شد — `pg_dump`/`pg_restore` + opt-in scheduler (از قبل) + حالا آپلود ابری خودکار به همون MinIO/S3 که RT-025/RT-030 استفاده می‌کنن (private، `backups/` prefix)، هم در scheduler و هم در CLI دستی. جزئیات در `DONE.md`                                                                                                                                          |  ✅   |
| RT-072 | SLA monitoring — فاز ۵ (هفته ۴۵-۴۶)                                                              | ✅ انجام شد — `o2n_uptime_seconds`/`o2n_health_check_status` + دو alert rule جدید. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                 |  ✅   |
| RT-073 | Database sharding strategy (سند طراحی) — فاز ۵ (هفته ۴۳-۴۴)                                      | ✅ انجام شد — فقط سند، طبق تصمیم صریح کاربر. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                       |  ✅   |
| RT-074 | Public API docs (Swagger) — فاز ۵ (هفته ۴۷-۴۸)                                                   | ✅ انجام شد — `@fastify/swagger`/`swagger-ui` روی spec موجود، سرو در `/docs`. یک باگ واقعی (YAML syntax) پیدا/فیکس شد. جزئیات در `DONE.md`                                                                                                                                                                                                                             |  ✅   |
| RT-075 | Tutorials — فاز ۵ (هفته ۴۷-۴۸)                                                                   | ✅ انجام شد — ۴ tutorial واقعی در `docs/tutorials/`. یک باگ واقعی (`skills:*` غایب از seed admin) پیدا/فیکس شد حین نوشتن. مستندسازی site کامل عمداً کنار گذاشته شد. جزئیات در `DONE.md`                                                                                                                                                                                |  ✅   |

## بخش B — تکمیلی: اکوسیستم Plugin (طبق جلسه ۴، `06_MEETINGS/04-plugin-ecosystem-architecture.md`)

| #      | تسک                                                                                                        | یادداشت                                                                                                                                                                                                                                                                                                                                                                 | وضعیت |
| ------ | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-076 | Per-plugin DB isolation — schema/namespace جدا داخل Postgres مشترک برای Pluginی که نیاز به ذخیره‌سازی دارد | ✅ انجام شد — `PluginSchemaService`، state از طریق request/response threading (نه callback API)، جزئیات در `DONE.md`                                                                                                                                                                                                                                                    |  ✅   |
| RT-077 | مسیر نصب self-hosted محلی برای Plugin (بدون نیاز به عبور از Marketplace)                                   | ✅ انجام شد — `local_plugins` + `LocalPluginService`، بدون نیاز به `MARKETPLACE_SERVICE_URL`، جزئیات در `DONE.md`                                                                                                                                                                                                                                                       |  ✅   |
| RT-078 | مسیر اجرای مستقیم Plugin تولیدشده توسط ایجنت برنامه‌نویس در Runtime                                        | ✅ انجام شد — CP-012 تکمیل شد، بلاک برداشته شد. `POST /v1/agents/:agentId/plugins/:pluginId/execute` (جدید، خارج از Workflow) — فقط برای agent با role=programmer + لایسنس managedAiGateway؛ منطق grant/invoke/state از RT-079's `executePluginStep` عیناً دوباره استفاده شد. تست HTTP واقعی: هم رد role غیر-programmer هم رد بدون‌لایسنس تأیید شد. جزئیات در `DONE.md` |  ✅   |
| RT-079 | نوع step جدید `plugin` در Workflow Engine (کنار `agent`/`tool`/`human`/`parallel`/`condition` موجود)       | ✅ انجام شد — محدود به Pluginهای thin HTTP-provider؛ اولین enforcement واقعی RT-080، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                |  ✅   |
| RT-080 | Per-agent Plugin grants (`agent_plugin_grants` + `PluginGrantService`)                                     | ✅ انجام شد — enforcement واقعی حالا در RT-079 وصل شد، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                              |  ✅   |

## بخش B۲ — مدل activation شخصی/سازمانی (جلسه ۵، `06_MEETINGS/05-self-service-signup-and-activation-model.md`)

| #      | تسک                                                                                                                                                          | یادداشت                                                                                   | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- | :---: |
| RT-081 | Activation check-in نوع/سقف seat رو از Control Plane می‌گیره؛ enforce سقف کاربر (شخصی=۱، سازمانی=seat limit)                                                 | ✅ انجام شد — `assertSeatAvailable` در UserService/InvitationService، جزئیات در `DONE.md` |  ✅   |
| RT-082 | در حالت activation شخصی، کل UI/routeهای Agent Access مخفی/غیرفعال بشه                                                                                        | ✅ انجام شد — `assertAgentAccessFeatureEnabled` + گیت UI، جزئیات در `DONE.md`             |  ✅   |
| RT-083 | چندزبانگی — `organizations.language` (پیش‌فرض) + `users.language` (override شخصی) + انتخاب زبان اولین ورود + JSON locale/AI-generation + RTL/LTR واقعی در چت | ✅ انجام شد — `/v1/locales/:lang` + `/v1/users/me` + dir واقعی در چت، جزئیات در `DONE.md` |  ✅   |

## بخش C — ⚠️ تصمیمات بزرگ‌تر معماری (نیاز به تأیید آگاهانه قبل از شروع)

| #      | تسک                                                                         | یادداشت                                                                                                                                                                                                                                             |
| ------ | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RT-011 | یکپارچگی واقعی Vault/secret manager (T-009)                                 | ✅ تصمیم گرفته شد و طراحی مشخص در `02_ARCHITECTURE/11-secrets-and-key-management.md` §4.1 — تفصیلی شده به **RT-019/RT-020** پایین                                                                                                                   |
| RT-012 | per-user credential واقعی (پسورد/توکن جدا به‌جای یک API key مشترک org-wide) | ✅ انجام شد — RT-014..RT-018 پایین همه ✅. `DEV_API_KEY` مشترک هنوز وجود داره اما حالا یکی از چند provider هست و پیش‌فرض production بی‌خطره (RT-018)                                                                                                |
| RT-013 | حافظه معنایی لایه‌های ۳-۶ + Memory Graph (Neo4j)                            | **این عملاً به‌جای اینجا، توی `TODO-openon4net-memory.md` بخش C (MEM-008/009/010) پیگیری می‌شه** — طبق `03-memory-engine.md` §8، لایه‌های بلندمدت می‌تونن به سرویس مدیریت‌شده (Plane 3) منتقل بشن. اینجا فقط برای یادآوری لینک شده، کار تکراری نیست |

---

## بخش D — Authentication Modes (از `02_ARCHITECTURE/16-authentication-modes.md`، 2026-07-10 اضافه شد)

> طراحی: یک Auth Method Registry provider-based — چند روش ورود می‌تونن هم‌زمان فعال باشن
> (`AUTH_METHODS_ENABLED` در env)، خروجی همه یکی‌ست (session/JWT واحد + همون RBAC فعلی).
> RT-014 زیرساخت پایه‌ست و بقیه بهش وابسته‌ن — ترتیب منطقی همون ترتیب شماره‌هاست.

| #      | تسک                                                                                                                                                                              | یادداشت                                                                                                                                                     | وضعیت |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-014 | Auth Method Registry (`AUTH_METHODS_ENABLED`/`AUTH_METHODS_DEFAULT`/`AUTH_ALLOW_DEV_METHODS` + provider interface + fail-fast اگه لیست خالیه + audit `auth_method` روی هر login) | ✅ انجام شد — `gateway/src/auth/registry.ts` + `env.ts`'s `superRefine`؛ `/v1/auth/methods` هم اضافه شد. جزئیات در `DONE.md`                                |  ✅   |
| RT-015 | Password provider (`argon2id`, `PASSWORD_MIN_LENGTH`, rate-limit/lockout روی login، مسیر `set-password` برای کاربرهای قدیمی بدون پسورد)                                          | ✅ انجام شد — `@node-rs/argon2`، Redis-backed lockout (۵ تلاش/۱۵ دقیقه)، جزئیات در `DONE.md`                                                                |  ✅   |
| RT-016 | Magic Link provider (`SMTP_*`, `EMAIL_FROM`, توکن یک‌بارمصرف کوتاه‌عمر قابل revoke)                                                                                              | ✅ انجام شد و end-to-end با یک MailHog واقعی تست شد (نه فقط کد) — جزئیات در `DONE.md`. پیش‌فرض غیرفعال چون SMTP واقعی نداریم                                |  ✅   |
| RT-017 | OAuth/OIDC provider (`OAUTH_PROVIDERS=google,github` + callback + map به user داخلی با email+subject)                                                                            | ✅ کد کامل، تست شده تا حدی که بدون یک Google/GitHub app واقعی ممکنه (مسیر start/state/callback واقعاً به Google زده شد) — همون بلاک RT-005، پیش‌فرض غیرفعال |  ✅   |
| RT-018 | سخت‌گیری Dev API Key (فقط با `AUTH_ALLOW_DEV_METHODS=true` و `NODE_ENV=development`؛ برای prod یا حذف کامل یا bootstrap-admin یک‌بارمصرف بعد disable)                            | ✅ انجام شد — یک باگ واقعی همینجا پیدا و فیکس شد (`z.coerce.boolean()` رو "false" رو `true` می‌خوند)، جزئیات در `DONE.md`                                   |  ✅   |

---

## بخش E — KMS Multi-Provider (از `02_ARCHITECTURE/11-secrets-and-key-management.md` §4.1، 2026-07-10 اضافه شد)

> طراحی: چند KMS provider می‌تونن هم‌زمان فعال باشن (`SECRETS_KMS_ENABLED_PROVIDERS`)، ولی نوشتن‌های
> جدید همیشه با provider «اصلی» (`SECRETS_KMS_PRIMARY_PROVIDER`) رمزنگاری می‌شن؛ خوندن بر اساس
> `provider_id` ذخیره‌شده در متادیتای خودِ هر secret انجام می‌شه (+ مسیر مهاجرت re-encrypt-on-read).

| #      | تسک                                                                                                                                     | یادداشت                                                                                                                                                                                                                                                                                                                                                                            | وضعیت |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-019 | KMS Provider Registry (env config + متادیتای `provider_id`/`key_id`/`version` روی هر secret رمزنگاری‌شده + fallback/re-encrypt-on-read) | ✅ انجام شد — `lib/kms/{types,env-provider,registry}.ts`؛ `keyId` = fingerprint خودِ کلید (نه برچسب نقشی «current/previous» که با rotation بعدی معنیش عوض می‌شد — یک باگ واقعی که در نوشتن تست پیدا و فیکس شد). `llm_configs`/`sso_configs` هر دو migrate شدن + re-encrypt-on-read. جایگزین RT-020 (Vault) نیست، ولی rotation واقعی رو بدون Vault ممکن می‌کنه. جزئیات در `DONE.md` |  ✅   |
| RT-020 | ⚠️ Vault provider واقعی (HashiCorp Vault Transit — `VAULT_ADDR`/`VAULT_TOKEN`/`VAULT_TRANSIT_KEY`)                                      | نیاز به یک **container جدید در `docker-compose.yml`** (وابستگی زیرساختی جدید برای یک محصول self-hosted) — این یکی هنوز نیاز به تأیید صریح داره قبل از شروع، چون نصب کاربر نهایی رو پیچیده‌تر می‌کنه                                                                                                                                                                                |  ❌   |

---

## بخش F — فرایند فکری قابل‌مشاهده Agent (از گفتگوی تعریف Agent، ۲۰۲۶-۰۷-۱۷)

> پس‌زمینه: در بازبینی تعریف رسمی Agent (`00_VISION/06-glossary.md`) مشخص شد نقش، مهارت (با
> permission)، ابزار از طریق پلاگین (با permission)، ارتباط Agent-to-Agent (`agent_messages` —
> واقعاً یک نوبت چت کامل روی Agent گیرنده اجرا می‌شه، نه فقط صندوق پیام)، و مشارکت در Workflow
> (step از نوع `agent`) همگی از قبل ساخته و تست شدن. تنها خلأ واقعی: مدل فعلاً فقط **پاسخ نهایی**
> رو ذخیره/نمایش می‌ده، نه مراحل استدلال میانی («فرایند فکری»).

| #      | تسک                                                                                                                                      | یادداشت                                                                                                                                                                                                                                                                                                                                                                           | وضعیت |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-084 | فرایند فکری قابل‌مشاهده — capture و ذخیره‌ی reasoning/extended-thinking مدل (جدا از پاسخ نهایی) + نمایش در UI چت، مستقل از پیام‌های عادی | ⚠️ انجام شد (subset واقعی) — تصمیم ذخیره‌سازی: همون جدول `messages` با نقش جدید `'thought'` (نه جدول جدا)، چون `role` یک `VARCHAR(20)` بدون CHECK constraint بود. Anthropic extended-thinking پیاده نشد (SDK نصب‌شده خیلی قدیمیه، جزئیات در `DONE.md`)؛ مسیر واقعی و کاری: `reasoning_content` روی providerهای OpenAI-compatible (DeepSeek reasoner و مشابه). جزئیات در `DONE.md` |  ⚠️   |

---

## بخش G — خلأهای معنای دقیق Agent (جلسه ۶، `06_MEETINGS/06-agent-definition-review-and-runtime-gaps.md`)

> پس‌زمینه: بازبینی تعریف رسمی Agent روشن کرد فقط RT-084 کافی نیست — خودِ حلقه‌ی
> ادراک/تصمیم/عمل هنوز native tool-calling نداره (اجرای ابزار کاملاً جدا از تصمیم مدله).
> دو تصمیم قبلی هم بازبینی شد: «Model Router چندمدلی» (۲۰۲۶-۰۷-۱۱) دوباره تأیید شد (کار
> Control Plane می‌مونه، اینجا ثبت نشد)، ولی تصمیم WebSocket (همون تاریخ) **معکوس شد**.

| #      | تسک                                                                                                                                                                              | یادداشت                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | وضعیت |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-085 | Native tool/skill-calling loop در چت (agentic/ReAct) — مدل وسط مکالمه تصمیم بگیرد ابزار/Skill صدا بزند، نتیجه در چت دیده شود و در Memory ذخیره شود                               | ✅ انجام شد (subset: دو Tool موجود — telegram-send/webhook-send، نه Skillهای دلخواه) — `@o2n/llm-providers`'s `LlmToolDefinition`/`LlmToolCall` (هم OpenAI-compatible هم Anthropic wiring شدن)، `chat-service.ts`'s `runToolLoop()` تا ۵ دور تصمیم‌گیری (`MAX_TOOL_ITERATIONS`)، فقط Toolهایی که خودِ کاربر RBAC permission‌شون رو داره advertise می‌شن (`agentic-tools.ts`). Policy gate (RT-008/RT-056) هنوز رعایت می‌شه — اکشن نیازمند تأیید اجرا **نمی‌شه** (بدون مکانیزم resume-بعد-از-تأیید، به مدل گفته می‌شه نیاز به تأیید دستی داره). ردیف `tool` جدید در تاریخچه (metadata شامل نام/آرگومان/نتیجه) — هرگز replay نمی‌شه به مدل در turn بعدی (مثل `thought`). تست واقعی: ۶ تست جدید `chat-service.test.ts` (وب‌هوک واقعی به postman-echo.com، policy gate، نام Tool نامعتبر، safety cap) با fake provider کنترل‌شده + ۵ تست HTTP واقعی جدید در `llm-providers` (۳ OpenAI-compat، ۲ Anthropic) که پروتکل سیمی واقعی هر دو ارائه‌دهنده رو با پاسخ‌های به‌درستی‌شکل‌دهی‌شده تأیید می‌کنن. **⚠️ محدودیت شناخته‌شده:** مدل‌های Ollama محلی موجود (`qwen2.5-coder:14b`، هم از `/v1/chat/completions` هم از `/api/chat` بومی خودِ Ollama) عملاً `tool_calls` واقعی برنمی‌گردونن — فقط متن JSON‌شکل داخل `content` می‌نویسن؛ این یک محدودیت واقعی مدل/دیپلوی محلیه (قالب چت این مدل خاص از tool-calling پشتیبانی نمی‌کنه)، نه باگ کد — کلید API واقعی OpenAI/Anthropic برای تست زنده‌ی کامل در این محیط موجود نبود. جزئیات کامل در `DONE.md` |  ⚠️   |
| RT-086 | Agent-to-agent delegation خودکار — وقتی مهارت/ابزار لازم به خودِ Agent grant نشده ولی Agent دیگری دارد، درخواست انجام کار بده                                                    | ✅ انجام شد — روی همون حلقه‌ی RT-085 ساخته شد: هر Skill فعال سازمان (نه فقط ۲ Tool ثابت) به مدل به‌عنوان تابع قابل‌فراخوانی نشون داده می‌شه، صرف‌نظر از اینکه خودِ Agent فعلی اون Skill رو grant داره یا نه — چک grant و delegation خودکار موقع اجراست، نه موقع تبلیغ (این یعنی مدل اصلاً لازم نیست از delegation خبر داشته باشه). اگه Agentِ فعلی grant داشت مستقیم اجرا می‌شه؛ اگه نداشت، `SkillGrantService.findGrantedAgent()` (کوئری معکوس جدید) یک Agent دیگه‌ی سازمان با grant پیدا می‌کنه، یک ردیف واقعی `agent_messages` (از Agent فعلی به delegate) برای audit/inbox ثبت می‌شه، Skill سینکرون روی delegate اجرا می‌شه (نه از طریق چرخه‌ی async فعلی زمان‌بند پیام‌ها، چون این turn به نتیجه‌ی فوری نیاز داره)، و پیام delivered/failed می‌شه. اگه هیچ Agent‌ای grant نداشت، خطای روشن به مدل برمی‌گرده. UI بلوک tool-call رو با «(delegated to X)» نشون می‌ده. تست واقعی: ۳ تست جدید (اجرای مستقیم، delegation واقعی با یک Agent دوم واقعی و ردیف agent_messages واقعی، عدم وجود هیچ Agent واجد شرایط) + smoke test واقعی با gateway واقعی (۲ Agent، Skill، grant، رد مسیر HTTP مستقیم). جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                        |  ✅   |
| RT-087 | Skillها مطابق استاندارد باز **Agent Skills** (`agentskills.io`) بازطراحی شوند — فرم no-code هم فراتر از webhook/telegram گسترش پیدا کنه، برای کارمند غیرفنی هم قابل‌استفاده باشه | ✅ انجام شد (subset تأییدشده‌ی کاربر، ۲۰۲۶-۰۷-۱۸: **فقط دستورالعمل**، بدون اجرای script — یک تصمیم امنیتی جدا) — جدول کاملاً جدید `agent_skill_packages` (+ `agent_skill_package_grants`)، **افزوده به کنار** مدل JSON قدیمی (`skills`/`SkillDefinitionSchema`)، نه جایگزینش — RT-085/RT-086 دست‌نخورده موندن. هر Skill package یک `name`+`description` (لایه‌ی «discovery»، همیشه به مدل نشون داده می‌شه) + `instructions` (متن مارک‌داون، لایه‌ی «activation» — فقط وقتی مدل واقعاً یک Skill رو granted داره صدا می‌زنه بارگذاری می‌شه، از طریق همون حلقه‌ی agentic RT-085 به‌عنوان یک تابع فقط-خواندنی بدون side effect). فرم no-code واقعی (Name/Description/Instructions) + مسیر «Import از یک فایل SKILL.md خام» (پارس frontmatter دستی، بدون وابستگی yaml جدید). جزئیات کامل + محدودیت صریح (بدون scripts/references/assets) در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |  ✅   |
| RT-088 | Agent Schedule غنی‌تر — انتخاب tool/skill/workflow هدف + الگوی ساعت/روز/ماه (نه فقط interval) + مفهوم timezone (سازمانی، مشابه الگوی زبان RT-083)                                | ✅ انجام شد — کاملاً افزوده به RT-007، نه جایگزینی: فیلدهای قدیمی (`intervalMinutes`/`prompt`) دست‌نخورده موندن، `target`/`timing` جدید و اختیاری‌ان. `target`: `chat` (رفتار قدیمی) / `tool` (تلگرام یا webhook مستقیم) / `skill` / `workflow`. `timing`: `interval` (رفتار قدیمی، همون شکل جدید) / `cron` (minute + hour اختیاری + daysOfWeek اختیاری + dayOfMonth اختیاری — یک subset دوستانه no-code، نه cron syntax کامل). `organizations.timezone` (پیش‌فرض UTC، اعتبارسنجی واقعی با `Intl.supportedValuesOf('timeZone')`، mirror دقیق الگوی `language` RT-083) الگوی cron رو با زمان محلی سازمان می‌سنجه. تست‌های واقعی: ۱۲ تست خالص برای `isDue()` (رفتار legacy، cron با minute/hour/daysOfWeek/dayOfMonth، جلوگیری از دوباره‌اجرایی توی همون پنجره‌ی یک‌دقیقه‌ای، **تبدیل timezone واقعی** Asia/Tehran در برابر UTC) + ۲ تست `OrgService` برای timezone + یک smoke test **واقعی زنده**: یک schedule cron واقعی روی گیت‌وی واقعی تنظیم شد (minute/hour دقیقاً برابر با «همین الان»)، ۴۰ ثانیه صبر شد، و scheduler واقعی (تیک ۳۰ثانیه‌ای) تارگت `tool` (webhook-send) رو واقعاً به postman-echo.com اجرا کرد و `lastRunAt` رو ست کرد. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                             |  ✅   |
| RT-089 | فیکس تنظیمات LLM provider — apiKey برای ollama اختیاری شود + لیست مدل‌های واقعی هر provider (نه input آزاد)                                                                      | ✅ انجام شد — `LlmConfigSetSchema`'s `superRefine` (اختیاری فقط برای ollama)، `GET /v1/config/models` (curated برای anthropic/openai/deepseek، **واقعاً دینامیک** برای ollama از `/api/tags`)، UI جایگزین input آزاد با select. تست HTTP واقعی با یک Ollama واقعی روی دستگاه. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |  ✅   |
| RT-090 | جایگزینی SSE با WebSocket برای استریم چت + رویدادهای real-time اجرای Tool/Skill (RT-085)                                                                                         | ✅ انجام شد برای استریم چت — `GET /v1/agents/:id/chat/ws` (`@fastify/websocket`)، توکن/organizationId به‌عنوان query param (مرورگر نمی‌تونه هدر سفارشی روی WebSocket بذاره)، فقط برای درخواست upgrade واقعی پذیرفته می‌شه (`plugins/auth.ts`). یک اتصال چند-turn (چند پیام روی یک سوکت، با guard جلوی race). یک باگ واقعی پیدا/فیکس شد: `requirePermission` به‌صورت غیر-async به‌عنوان `preHandler` روی مسیر websocket **بی‌نهایت hang** می‌کرد (Fastify منتظر یک Promise یا callback `done()` می‌مونه که هیچ‌کدوم برنمی‌گرده) — فیکس با wrap کردن در یک تابع async. تست real end-to-end با یک Ollama واقعی + تست route-level با `injectWS()`. رویدادهای real-time اجرای Tool (نیمه‌ی دوم) حالا با RT-085 اضافه شدن — همون کانال WS، فریم‌های `tool_call`/`tool_result` جدید، بدون تغییر پروتکل. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |  ✅   |
| RT-091 | i18n کامل UI (همه‌ی منوها/صفحات)، نه فقط چت — گسترش RT-083                                                                                                                       | ⚠️ کامل نشد (subset واقعی) — RT-083 خودِ `GET /v1/locales/:lang` رو ساخته بود ولی **هیچ صفحه‌ای اصلاً صداش نمی‌زد** (کد مرده). حالا `useLocaleStrings()` (زیرساخت واقعی مصرف) + Sidebar (nav روی همه‌ی صفحات) + چت (بقیه‌ی رشته‌ها) واقعاً ترجمه می‌شن. باقی صفحات (Skills, Marketplace, Users, Roles, Policies, Workflows, ...) هنوز متن انگلیسی hardcode دارن — جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |  ⚠️   |
| RT-092 | صفحه‌ی first-run برای وارد کردن activation code (به‌جای فقط env var دستی)                                                                                                        | ✅ انجام شد — جدول singleton `activation_config` (id=1، CHECK)، کلید فعال‌سازی envelope-encrypted با همون الگوی KMS registry (مثل `llm_configs`/`sso_configs`)، `POST /v1/activation/configure` قبل از ذخیره یک check-in واقعی به Platform می‌زنه (کد نامعتبر هرگز ذخیره نمی‌شه)، `GET /v1/activation/status`، بخش «Activation» در `web/app/settings/page.tsx`. تست end-to-end واقعی (Runtime↔Platform روی پورت‌های واقعی). جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |  ✅   |

---

## بخش H — عبور اجباری از Platform (جلسه ۷، `06_MEETINGS/07-platform-rename-and-mediated-cross-plane-access.md`)

> پس‌زمینه: Runtime دیگه نباید مستقیم به Marketplace وصل بشه — همه چیز باید از Platform
> (نام جدید Control Plane) عبور کنه، هم برای یک لایه‌ی امنیتی هم برای ثبت میزان مصرف.
> **گاردریل برای آینده:** هر integration جدیدی که با سرویس Memory ساخته بشه (هنوز هیچ تسکی
> براش ثبت نشده) هم باید از همون ابتدا از طریق Platform باشه، نه مستقیم.

| #      | تسک                                                                                                                   | یادداشت                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | وضعیت |
| ------ | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---: |
| RT-093 | مهاجرت مشتری Marketplace موجود در Runtime (`marketplace-client.ts`) از تماس مستقیم به تماس proxy-شده از طریق Platform | ✅ انجام شد — `marketplaceRequest()` وقتی `securityToken` (از CP-032) و `CONTROL_PLANE_URL` هر دو موجود باشن از `${CONTROL_PLANE_URL}/v1/proxy/...` عبور می‌کنه، وگرنه fallback به تماس مستقیم قدیمی با `MARKETPLACE_API_KEY` (سازگار با self-host بدون activation). فقط ۹ متد authenticated (install/rate/publish/config) توکن می‌گیرن؛ ۴ متد discovery فقط-خواندنی (listPlugins/listSkills/getPlugin/getSkill) همیشه مستقیم می‌مونن چون endpointهای browse عمومی و بدون auth هستن. تست end-to-end واقعی: Runtime→Platform(CP-034 proxy)→fake-Marketplace با موفقیت کامل (بدون نیاز Runtime به `MARKETPLACE_API_KEY` محلی). Memory از scope این تسک خارج شد چون Runtime هنوز هیچ client‌ای برای Memory نداره که مهاجرت بشه. جزئیات در `DONE.md` |  ✅   |

---

## بخش I — بازطراحی UI/UX (نقد کاربر ۲۰۲۶-۰۷-۱۸: «از نظر ui/ux اصلا قابل قبول نیست»)

> پس‌زمینه: صفحات فعلی وب Runtime (`/agents`, `/skills`, `/settings`, `/users`, `/chat`, ...)
> صرفاً functional ساخته شدن — inline style خام، بدون design system، بدون palette/typography
> ثابت. کاربر نقد کرد و بعد از تأیید، skill پلاگین `ui-ux-pro-max` رو نصب کرد (در دسترس Claude
> از ۲۰۲۶-۰۷-۱۸). ترتیب اجرا عمداً «اول token، بعد صفحه‌به‌صفحه» است تا یک زبان طراحی واحد اول
> شکل بگیره، نه رنگ‌آمیزی پراکنده هر صفحه جدا.
>
> **بروزرسانی همون روز:** بعد از تکمیل RT-094/095/096 (token + `TopBar`)، کاربر یک مدل
> layout متفاوت خواست — sidebar کشویی به‌جای نوار بالا، یک صفحه‌ی Dashboard واقعی (نه فقط
> لیست Agent)، و PWA/responsive. RT-097..RT-100 پایین این‌ها رو ثبت می‌کنن؛ RT-097 منطق
> `TopBar.tsx` (لینک‌ها، gating، active-state) رو دوباره استفاده می‌کنه، فقط container عوض
> می‌شه — کار RT-095/096 دور ریخته نمی‌شه.

| #      | تسک                                                                                                                                              | یادداشت                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-094 | Design System pass — token رنگ/تایپوگرافی/spacing واحد برای کل وب Runtime با `ui-ux-pro-max`                                                     | ✅ انجام شد — `globals.css` سه‌لایه (primitive→semantic→component)، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                                      |  ✅   |
| RT-095 | اعمال Design System روی صفحات پرترافیک — Chat (۳ پنل) + Agents dashboard                                                                         | ✅ Agents انجام شد (token + sidebar، بعد از RT-097 جایگزین `TopBar`). Chat هم امروز کامل شد — همه‌ی مقادیر خام px در `style={{...}}` (fontSize/gap/margin/padding) با token‌های `--font-size-*`/`--space-*` جایگزین شدن (به‌جز یک استثنای آگاهانه: `marginTop: 2`، چون کوچک‌تر از کوچک‌ترین token یعنی `--space-1`=4px است). رنگ‌ها از قبل token بودن. build/typecheck/lint واقعاً سبز؛ امروز با Playwright واقعی (headless Chromium) هم بصری تأیید شد — جزئیات در `DONE.md` |  ✅   |
| RT-096 | اعمال Design System روی بقیه صفحات — Skills, Settings, Users, Roles, Policies, Workflows, Marketplace, Approvals, Outcomes, Audit                | ✅ انجام شد — همه‌ی ۱۶ صفحه (به‌جز Chat) روی token منتقل شدن، بعداً nav به sidebar (RT-097) عوض شد، جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                      |  ✅   |
| RT-097 | جایگزینی nav بالای صفحه (`TopBar`) با یک **sidebar کشویی** (drawer، collapsible) — همون لیست لینک/gating که RT-095/096 ساختن، فقط با layout جدید | ✅ انجام شد — `Sidebar.tsx` (جایگزین `TopBar.tsx`، حذف شد)؛ collapse دسکتاپ (localStorage) + drawer/overlay موبایل (زیر ۹۰۰px) + backdrop. همه‌ی ۱۸ صفحه (Chat هم) منتقل شدن. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                            |  ✅   |
| RT-098 | صفحه‌ی Dashboard واقعی (overview: خلاصه Agentها، بودجه، KPIهای کلی، فعالیت اخیر) — جدا از لیست ساده‌ی فعلی `/agents`                             | ✅ انجام شد — `/dashboard` (کارت Agents/Budget/Approvals/Wallet + جدول فعالیت اخیر)، اضافه به sidebar. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                   |  ✅   |
| RT-099 | PWA — `manifest.json` + service worker + قابل نصب (installable) روی موبایل/دسکتاپ                                                                | ✅ انجام شد — `manifest.json` + `sw.js` (API هیچ‌وقت cache نمی‌شه) + آیکون placeholder. جزئیات در `DONE.md`                                                                                                                                                                                                                                                                                                                                                                  |  ✅   |
| RT-100 | Responsive pass — breakpoint موبایل/تبلت برای همه‌ی صفحات + خودِ sidebar جدید (RT-097)                                                           | ✅ انجام شد — بازبینی واقعی با Playwright (headless Chromium، نصب مستقیم پکیج، نه MCP) روی ۱۶ صفحه × ۴ عرض viewport (۶۴ اسکرین‌شات واقعی). ۴ باگ واقعی پیدا و فیکس شد؛ جزئیات کامل در `DONE.md`                                                                                                                                                                                                                                                                              |  ✅   |

---

## بخش J — سمت Runtime برای AI Gateway مدل OpenRouter (جلسه ۸، `06_MEETINGS/08-ai-gateway-openrouter-model.md`)

> پس‌زمینه کامل و تسک‌های سمت Platform در `TODO-openon4net-platform.md` بخش G
> (CP-035..CP-044). این ردیف‌ها فقط سمت Runtime‌ان — بلاک روی ساخته‌شدنِ کامل
> توکن AI Gateway در Platform (CP-036).

| #      | تسک                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | یادداشت                                                                                                                | وضعیت |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-101 | توکن «AI Gateway» — یک تنظیم **پیش‌فرض org-wide** (سازمان کلی) + یک override **اختیاری به‌ازای هر Agent**؛ Agentهای بدون توکن اختصاصی از پیش‌فرض استفاده می‌کنن. هر توکن (پیش‌فرض یا per-agent) یک کلید دستی است که کاربر در Platform می‌سازه و در Runtime پیست می‌کنه — Runtime چیزی خودش نمی‌سازه/درخواست نمی‌ده. علاوه بر این، هر توکن یک **حالت انتخابی** داره: BYOK مستقیم (provider خودِ سازمان، معماری فعلی Runtime) یا AI Gateway مدیریت‌شده‌ی Platform — این دو مسیر کنار هم می‌مونن، جایگزین هم نمی‌شن | تصمیم جلسه ۸ (۶.۵): صدور توکن **دستی**، نه خودکار — بلاک روی CP-036                                                    |  ⏳   |
| RT-102 | نمایش provider/model مصرفی هر پیام در UI چت، وقتی Agent از AI Gateway استفاده می‌کنه — تا مدیر Runtime بفهمه پرمصرف‌ترین provider هر Agent کدومه                                                                                                                                                                                                                                                                                                                                                                 | گسترش همون الگوی متادیتای پیام که برای RT-085 (tool calls) و RT-084 (reasoning) ساخته شد؛ بلاک روی RT-101 + CP-036/038 |  ⏳   |

---

---

## بخش K — Workflow Engine: پارَ n8n (جلسه ۹، `06_MEETINGS/09-workflow-engine-n8n-parity.md`)

> پس‌زمینه کامل بحث/تصمیمات در فایل جلسه بالا. خلاصه: Workflow Engine فعلی برای هدف محدودش
> (زنجیره‌ی از‌پیش‌طراحی‌شده‌ی Agent→Tool→Approval) کار می‌کنه، ولی برای ادعای «قدرت n8n» یا
> اجرای بی‌نظارت مقیاس‌پذیر ۴ شکاف داره. اولویت پیشنهادی: اول RT-103..106 (فنی/کم‌ریسک + مسیر
> میان‌بر برای ساخت با Agent)، بعد RT-107 (canvas واقعی، فاز بزرگ‌تر و جدا).

| #      | تسک                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | یادداشت                                                                                                                                                                                                                                     | وضعیت |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-103 | نوع step جدید `loop` در `WorkflowStepSchema` (`packages/shared/src/schemas/workflow.ts`) — روی یک آرایه از context اجرا (`items: string` مسیر dot-path به یک آرایه در context، `steps: WorkflowStep[]` زیرمجموعه‌ای که به‌ازای هر عضو اجرا می‌شه، `maxIterations` سقف امنیتی اجباری تا یک آرایه‌ی بزرگ/بی‌نهایت گیت‌وی رو نخوابونه). تغییر متناظر در `WorkflowExecutor` برای اجرای iterative و جمع‌کردن نتایج هر iteration توی context زیر یک کلید مشخص (مثلاً `loopResults`). محدودیت: طبق تصمیم جلسه ۹، فقط sub-step های `agent`/`tool` مجازن داخل loop (نه `loop`/`parallel` تودرتو) — همون محدودیت کم‌عمق بودن که `parallel` هم داره، برای جلوگیری از پیچیدگی v1. | تست: حداقل یک workflow واقعی با ۳-۵ آیتم لیست، تأیید نتایج هر iteration در context، و رد شدن iteration بیشتر از `maxIterations` با خطای تمیز (نه crash/hang)                                                                                |  ⏳   |
| RT-104 | Retry policy per-step — فیلد اختیاری `retry: { maxAttempts: number, backoffMs: number }` روی هر step type (به‌جز `human`/`condition` که معنی نداره). `WorkflowExecutor` قبل از fail کردن یک step، تا `maxAttempts` بار با فاصله‌ی `backoffMs` (ضربدر تلاش، exponential ساده) دوباره امتحان می‌کنه؛ فقط برای خطاهای واقعاً transient (network/timeout روی `tool`/`plugin`) نه خطاهای validation. مهم‌تر شده چون الان trigger های `scheduled`/`webhook` داریم یعنی workflow بدون نظارت انسان اجرا می‌شه (طبق بحث جلسه ۹).                                                                                                                                               | تست: یک `webhook-send` که بار اول عمداً fail می‌شه (مثلاً به یک URL که موقتاً 503 می‌ده) و بار دوم موفق — تأیید که کل workflow fail نشد؛ و یک مورد که همه‌ی تلاش‌ها fail می‌شن و خطای تمیز نهایی برمی‌گرده                                  |  ⏳   |
| RT-105 | نوع step جدید `subworkflow` — `{ id, type: 'subworkflow', workflowId: string, inputMapping?: Record<string,string> }`. `WorkflowExecutor` وقتی به این step می‌رسه، یک `WorkflowExecutor` جدید برای `workflowId` مقصد می‌سازه (recursive)، نتیجه‌ی نهاییش رو توی context والد برمی‌گردونه. **جلوگیری از چرخه اجباری**: قبل از اجرا، زنجیره‌ی صدازدن‌ها چک بشه (A→B→A نباید اجرا بشه) — دقیقاً همون الگوی `assertNoReportsToCycle` که برای سلسله‌مراتب Agent (RT-045) از قبل هست، اینجا هم استفاده بشه، نه از صفر نوشته بشه.                                                                                                                                            | تست: یک زنجیره‌ی ۲-سطحی واقعی (workflow A یک step `subworkflow` داره که B رو صدا می‌زنه)، + یک تست صریح که تلاش برای ساختن چرخه (A→B→A) رد بشه با خطای تمیز، نه اجرای بی‌نهایت                                                              |  ⏳   |
| RT-106 | ساخت workflow با کمک Agent از زبان طبیعی — یک endpoint/چت جدید که کاربر توصیف می‌کنه چی می‌خواد («وقتی فرم پر شد، به Agent فروش بگو خلاصه بده و بعد از تأیید من به تلگرام بفرسته») و یک Agent (LLM با یک system prompt مخصوص + دسترسی به‌عنوان ابزار به همون `WorkflowDefinitionSchema`/step schema های موجود) خروجی رو به یک `WorkflowDefinition` معتبر ترجمه می‌کنه؛ خروجی حتماً با همون Zod schema اعتبارسنجی بشه قبل از ذخیره (LLM هیچ‌وقت مستقیم به DB نمی‌نویسه). محدودیت مستندشده (طبق جلسه ۹): فقط روی step typeهای موجود کار می‌کنه؛ منطق واقعاً جدید هنوز از مسیر `plugin`/WASM سندباکس (که هنوز ساخته نشده) عبور می‌کنه، این تسک اون رو تغییر نمی‌ده.      | تست: حداقل ۳ توصیف زبان طبیعی واقعی (نه fixture ساختگی) که هرکدوم به یک `WorkflowDefinition` معتبر و منطقاً درست ترجمه بشن؛ یک مورد که توصیف مبهم/غیرممکنه (مثلاً نیاز به loop نامحدود) و باید Agent شفاف رد کنه، نه یک workflow خراب بسازه |  ⏳   |
| RT-107 | Visual Builder — یک canvas واقعی (پیشنهاد: `@xyflow/react` یا مشابه) که هر step type رو به‌عنوان یک node قابل drag-and-drop نشون بده، پالت node از روی همون step schema های موجود (`WorkflowStepSchema`'s discriminated union) تولید بشه، فرم تنظیمات هر node بر اساس فیلدهای همون نوع step، و یال‌ها (edges) ترتیب/شرط بین stepها رو نشون بدن؛ sync دوطرفه با همون `WorkflowDefinition` JSON فعلی (نه جایگزینش، چون textarea/paste-JSON هنوز باید برای کاربر فنی/import-export بمونه). **فاز جدا و بزرگ‌تر** — طبق تصمیم جلسه ۹ بعد از RT-103..105 شروع بشه تا node-palette نهایی (شامل `loop`/`subworkflow`) باشه، نه اینکه دوباره‌کاری بشه.                        | این تسک قبل از شروع نیاز به یک دور طراحی/تأیید UI جدا داره (مثل چیزی که برای بخش I انجام شد) — صرفاً «شروعش کن» کافی نیست                                                                                                                   |  ⏳   |

---

## بخش L — Relay وب‌هوک عمومی از طریق Platform (جلسه ۱۰، `06_MEETINGS/10-public-webhook-relay-via-platform.md`)

> پس‌زمینه کامل و تسک‌های سمت Platform در `TODO-openon4net-platform.md` بخش H
> (CP-045..047). این ردیف‌ها فقط سمت Runtime‌ان.

| #      | تسک                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | یادداشت                                                                                                                                                               | وضعیت |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-108 | WebSocket client دائمی از Runtime به Platform (reconnect خودکار با backoff، مثل الگوی `activation-scheduler.ts` ولی یک اتصال باز به‌جای poll دوره‌ای) — با همون activation key احراز هویت می‌شه. وقتی یک payload relay‌شده (شامل `runtime_webhook_token`) می‌رسه، مستقیم همون منطق داخلی `POST /v1/webhooks/:token` (`webhook-endpoint-service.ts`'s `findByToken`/`markTriggered` + dispatch) رو صدا می‌زنه — به‌صورت یک فراخوانی تابع داخلی، نه یک HTTP round-trip واقعی به خودش | بلاک روی CP-046 (سرور WebSocket سمت Platform). فقط برای سازمان‌های activated معنی داره؛ اگه activation نباشه، این اتصال اصلاً برقرار نمی‌شه (بدون تلاش مکرر بی‌فایده) |  ⏳   |
| RT-109 | اضافه‌شدن `target_type: 'tool'` به `WebhookEndpointCreateSchema`/جدول `webhook_endpoints` — صدا زدن مستقیم یک tool از webhook، بدون پوشوندنش توی یک workflow تک‌قدمی. از `tool-dispatcher.ts`'s `executeTool(step, env)` مستقیم استفاده می‌کنه (یک `SkillStep` ساختگی از تنظیمات endpoint ساخته می‌شه) — نیازی به تغییر خودِ dispatcher نیست                                                                                                                                       | مستقل از RT-108 — به relay/Platform بستگی نداره، همین الان هم روی webhook مستقیم Runtime (بدون relay) قابل تست‌ه                                                      |  ⏳   |

---

## بخش M — اصلاح معماری ارتباطی (جلسه ۱۱، `06_MEETINGS/11-cross-plane-transport-architecture.md`)

> پس‌زمینه کامل در فایل جلسه بالا. کاربر معماری ۴ پروژه رو مرور کرد و دو مسیر ارتباطی رو
> «غلط» تشخیص داد — این بخش فقط سمت Runtime‌ان.

| #      | تسک                                                                                                                                                                                                                                                                                                          | یادداشت                                                                                                                                                    | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-110 | جدا کردن بخش‌های «زنده/بلادرنگ» `web/lib/api-client.ts` و انتقالشون به WebSocket (هیبرید — نه همه‌ی ۱۱۴+ متد، فقط این‌ها): وضعیت اجرای workflow run، وضعیت آنلاین/مشغول Agent، نوتیفیکیشن approval بلادرنگ. بقیه (CRUD معمولی) همون REST می‌مونه                                                             | تصمیم صریح کاربر: هیبرید نه بازنویسی کامل. لیست دقیق «زنده» بالا پیشنهاد Claude‌ه، قابل اضافه‌شدن قبل از شروع                                              |  ⏳   |
| RT-111 | حذف مسیر مستقیم `marketplace-client.ts` (بدون Platform) — `useProxy` دیگه شرطی نباشه، همیشه از طریق Platform's CP-034 proxy بره، حتی برای discovery (`GET /marketplace/plugins` که تا الان همیشه مستقیم بود). یعنی activation برای _هرگونه_ دسترسی به Marketplace (نه فقط install/rate/publish) اجباری می‌شه | تصمیم صریح کاربر: حذف بشه، نه نگه داشته بشه. باید مستندات RT-093 (که این مسیر مستقیم رو عمداً برای self-host بدون activation توجیه کرده بود) هم به‌روز بشه |  ⏳   |

---

## بخش N — LLM Provider و کانال‌ها به‌عنوان Plugin (جلسه ۱۲، `06_MEETINGS/12-plugin-based-llm-providers-and-channels.md`)

> پس‌زمینه کامل در فایل جلسه بالا. دو تصمیم بزرگ تأیید شد: (۱) anthropic/openai/deepseek
> از env-config به پلاگین مهاجرت می‌کنن، فقط Ollama (local/cloud) بدون‌پلاگین می‌مونه؛
> (۲) انتساب provider از سطح Organization به **per-Agent** منتقل می‌شه. تسک‌های سمت
> Platform (بسته‌بندی AI Gateway به‌عنوان پلاگین) در `TODO-openon4net-platform.md` بخش J.

| #      | تسک                                                                                                                                                                                                                                                                                                              | یادداشت                                                                                                                                                                                    | وضعیت |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---: |
| RT-112 | فیلد provider-plugin روی مدل Agent (فعلاً هیچ فیلد provider‌ای نداره) + یک قرارداد فنی جدید «Plugin LLM Provider» — متفاوت از پلاگین HTTP-tool موجود (RT-079، `manifest.provider: {type:'http', baseUrl}`) چون آداپتور completion API نیاز داره، نه صرفاً یک HTTP call عمومی                                     | پایه‌ی همه‌ی تسک‌های زیر این بخش — باید اول ساخته بشه                                                                                                                                      |  ⏳   |
| RT-113 | **(دامنه در جلسه ۱۴ محدود شد به فقط Anthropic)** مهاجرت آداپتور Anthropic از `env.ts`'s `LLM_PROVIDER` به پلاگین واقعی (روی قرارداد RT-112)                                                                                                                                                                      | وابسته به RT-112. Priority: P0، Size: M                                                                                                                                                    |  ⏳   |
| RT-114 | قرارداد عمومی «Channel Plugin (سمت Runtime)» — منطق واقعی bot/پاسخ‌گویی، رویداد ورودی رو از طریق Plugin Event Relay (CP-053) دریافت می‌کنه، نه مستقیم                                                                                                                                                            | پایه‌ی RT-115/116. **اصلاح جلسه ۱۵:** inbound handling واقعی دیگه اینجا نیست — سمت پلاگین Platform (CP-056..061) است، چون فقط اون یک endpoint عمومی داره؛ اینجا فقط مصرف‌کننده‌ی relay است |  ⏳   |
| RT-115 | تأیید اجباری اتصال کانال خارجی — وقتی یک فرستنده‌ی خارجی جدید (مثلاً یک chat_id تلگرام) برای اولین‌بار پیام می‌ده، قبل از پاسخ‌گویی باید توسط ادمین سازمان تأیید بشه. **بدون قابلیت خاموش‌کردن — همیشه اجباریه** (اصلاح‌شده در جلسه ۱۲: این فقط برای کانال‌های خارجی صادقه، نه اتصال وب/اپ — اون یکی RT-117 است) | وابسته به RT-114                                                                                                                                                                           |  ⏳   |
| RT-116 | Device↔Agent binding برای فرستنده‌های خارجی — رابطه‌ی many-to-many بین یک فرستنده‌ی تأییدشده (RT-115) و Agentهایی که مجاز به گفتگو باهاشن                                                                                                                                                                        | وابسته به RT-115                                                                                                                                                                           |  ⏳   |
| RT-117 | تأیید device برای اتصال وب/اپلیکیشن — یک تنظیم سازمانی جدید (`requireDeviceApproval`)، **پیش‌فرض خاموش**؛ اگه ادمین روشنش کنه، از اون به بعد device/session جدید (مرورگر/اپ) قبل از اتصال باید توسط ادمین تأیید بشه                                                                                              | مستقل از RT-114..116 (این مال وب/اپ است، نه کانال خارجی) — جزئیات فنی «چی device رو تشخیص می‌ده» (fingerprint مرورگر یا صرفاً token جدید) هنوز باز، جلسه ۱۲                                |  ⏳   |

> **جلسه ۱۴ (`06_MEETINGS/14-llm-provider-plugin-granularity.md`)** — RT-113 محدود به
> Anthropic شد؛ ۱۰ تسک زیر برای بقیه‌ی providerهای لیست جلسه ۸ اضافه شدن. همه وابسته
> به RT-112. اولویت/سایز نتیجه‌ی جلسه ۱۴ موضوع ۳ است.

| #      | تسک                                                                                         | یادداشت                                                                                       | وضعیت |
| ------ | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | :---: |
| RT-122 | OpenAI Provider Plugin — مهاجرت از `env.ts`'s `LLM_PROVIDER` به پلاگین (روی قرارداد RT-112) | وابسته به RT-112. Priority: P0، Size: M                                                       |  ⏳   |
| RT-123 | DeepSeek Provider Plugin — مهاجرت از `env.ts`'s `LLM_PROVIDER` به پلاگین                    | وابسته به RT-112. Priority: P1، Size: S                                                       |  ⏳   |
| RT-124 | Groq Provider Plugin (جدید — provider جدید، امروز پشتیبانی نمی‌شه)                          | وابسته به RT-112. Priority: P1، Size: S                                                       |  ⏳   |
| RT-125 | Google Gemini Provider Plugin (جدید)                                                        | وابسته به RT-112. پروتکل native خودش، نه لزوماً OpenAI-compatible کامل. Priority: P1، Size: M |  ⏳   |
| RT-126 | Mistral Provider Plugin (جدید)                                                              | وابسته به RT-112. Priority: P2، Size: S                                                       |  ⏳   |
| RT-127 | Azure OpenAI Provider Plugin (جدید)                                                         | وابسته به RT-112. Priority: P2، Size: S                                                       |  ⏳   |
| RT-128 | ~~Together AI Provider Plugin~~ — **حذف شد (جلسه ۱۵، درخواست صریح کاربر)**                  | —                                                                                             |  ❌   |
| RT-129 | Fireworks Provider Plugin (جدید)                                                            | وابسته به RT-112. Priority: P2، Size: S                                                       |  ⏳   |
| RT-130 | Perplexity Provider Plugin (جدید)                                                           | وابسته به RT-112. Priority: P2، Size: S                                                       |  ⏳   |
| RT-131 | Cohere Provider Plugin (جدید)                                                               | وابسته به RT-112. پروتکل native خودش. Priority: P2، Size: M                                   |  ⏳   |

## بخش O — تقویت enforcement استاندارد manifest پلاگین (جلسه ۱۳، `06_MEETINGS/13-plugin-manifest-standard-enforcement.md`)

> پس‌زمینه: استاندارد manifest از قبل مستند بود (`02_ARCHITECTURE/03-skill-engine.md` بخش ۵)
> و CLI (`create-o2n-plugin`) هم واقعیه، ولی هیچ‌جا سمت سرور واقعاً enforce نمی‌شد —
> هر سه مسیر (نصب لوکال Runtime، آپلود zip Runtime، submit Marketplace) خودِ manifest رو
> یک `z.record(z.unknown())` بدون schema قبول می‌کردن. تسک‌های سمت Marketplace در
> `TODO-openon4net-marketplace.md` بخش I.

| #      | تسک                                                                                                                                                                                                                                                                                   | یادداشت                                                                                                                                       | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | :---: |
| RT-119 | تعریف `PluginManifestSchema` (Zod) در `@o2n/plugin-sdk` — تمام فیلدهای بخش ۵ سند معماری + `schemaVersion` جدید + semver واقعی روی `version` + enum بسته برای `permissions[]`/`hooks[]` + **(اضافه‌شده جلسه ۱۵) `keywords: string[]` آزاد + `installTarget: 'platform' \| 'runtime'`** | پایه‌ی همه‌ی تسک‌های زیر این بخش — باید اول ساخته بشه. `@o2n/plugin-sdk` الان توسط هیچ پکیجی مصرف نمی‌شه؛ این اولین مصرف‌کننده‌ی واقعیش می‌شه |  ⏳   |
| RT-118 | ثبت `apps/openon4net-marketplace/service` در root `pnpm-workspace.yaml` — تا بتونه `@o2n/plugin-sdk` رو `workspace:*` import کنه                                                                                                                                                      | **تغییر فایل root** — طبق تصمیم صریح جلسه ۱۳ موضوع ۱ مجاز شد (نه یک استثنای عمومی برای تغییرات root آینده)                                    |  ⏳   |
| RT-120 | اعمال `PluginManifestSchema` روی مسیرهای نصب Runtime (`routes/local-plugins.ts`: هم نصب لوکال هم آپلود zip) — رد با خطای ۴۰۰ تمیز به‌جای پذیرش blob                                                                                                                                   | وابسته به RT-119                                                                                                                              |  ⏳   |
| RT-121 | گسترش CLI (`create-o2n-plugin`) با یک دستور `validate` که همون schema رو روی یک پوشه‌ی پلاگین محلی اجرا می‌کنه، پیش از publish                                                                                                                                                        | وابسته به RT-119                                                                                                                              |  ⏳   |

## بخش P — Media generation providers + Channel plugins سمت Runtime (جلسه ۱۵، `06_MEETINGS/15-media-channel-google-plugins-and-blog.md`)

> پس‌زمینه کامل در فایل جلسه بالا. تسک‌های متناظر سمت Platform در
> `TODO-openon4net-platform.md` بخش K، سمت Marketplace در
> `TODO-openon4net-marketplace.md` بخش J.

| #      | تسک                                                                                                                                               | یادداشت                                                                                                   | وضعیت |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | :---: |
| RT-132 | گسترش enum دسته‌بندی پلاگین (`local-plugin-categories.ts`): افزودن `llm-provider`, `media-generation`, `connector` (۷→۱۰ مقدار) + فیلد `keywords` | باید هم‌گام با MKT-031 (Marketplace) انجام بشه، طبق الگوی migration 0006 موجود                            |  ⏳   |
| RT-133 | قرارداد جدید «Media Generation Provider Plugin» — prompt-in/asset-out، پشتیبانی از polling غیرهمزمان (برای ویدیو)                                 | متفاوت از قرارداد متنی RT-112 — پایه‌ی RT-134..139                                                        |  ⏳   |
| RT-134 | OpenAI (DALL-E) Image Provider Plugin                                                                                                             | وابسته به RT-133. جدا از RT-122 (پلاگین متنی OpenAI) — قرارداد متفاوت، حتی provider یکسان                 |  ⏳   |
| RT-135 | Stability AI Image Provider Plugin                                                                                                                | وابسته به RT-133                                                                                          |  ⏳   |
| RT-136 | Runway Video Provider Plugin                                                                                                                      | وابسته به RT-133. احتمالاً نیاز به job/polling async                                                      |  ⏳   |
| RT-137 | Luma Video Provider Plugin                                                                                                                        | وابسته به RT-133. احتمالاً نیاز به job/polling async                                                      |  ⏳   |
| RT-138 | ElevenLabs Voice Provider Plugin                                                                                                                  | وابسته به RT-133                                                                                          |  ⏳   |
| RT-139 | OpenAI TTS/Whisper Audio Provider Plugin                                                                                                          | وابسته به RT-133. جدا از RT-122/RT-134                                                                    |  ⏳   |
| RT-140 | Telegram Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace                                                                                  | وابسته به RT-114 (اصلاح‌شده) + CP-053 (relay) + CP-056 (نیمه‌ی Platform)                                  |  ⏳   |
| RT-141 | WhatsApp Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace                                                                                  | وابسته به RT-114 + CP-053 + CP-057                                                                        |  ⏳   |
| RT-142 | Discord Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace                                                                                   | وابسته به RT-114 + CP-053 + CP-058                                                                        |  ⏳   |
| RT-143 | Google Chat Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace                                                                               | وابسته به RT-114 + CP-053 + CP-059                                                                        |  ⏳   |
| RT-144 | WeChat Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace                                                                                    | وابسته به RT-114 + CP-053 + CP-060. پیچیدگی احتمالی: تأیید هویت کسب‌وکار مخصوص چین                        |  ⏳   |
| RT-145 | ⚠️ iMessage Channel Plugin (سمت Runtime) — **نیاز به تحقیق feasibility قبل از build**                                                             | هیچ API عمومی/Business رسمی اپل نداره؛ نیاز به Mac واقعی+AppleScript یا سرویس واسط شخص‌ثالث با شرایط مبهم |  ❓   |

> **افزوده‌ی همون روز (جلسه ۱۵، بخش ۶):** Facebook Messenger و Instagram DM
> واقعاً conversational‌ان → همون الگوی جفت. TikTok/LinkedIn/YouTube پلتفرم
> انتشار محتوان نه گفتگو → فقط یک پلاگین Platform-side دارن (`TODO-openon4net-platform.md`
> بخش K)، بدون جفت Runtime.

| RT-146 | Facebook Messenger Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace | وابسته به RT-114 + CP-053 + CP-062 | ⏳ |
| RT-147 | Instagram DM Channel Plugin (سمت Runtime) — قابل‌فروش در Marketplace | وابسته به RT-114 + CP-053 + CP-063 | ⏳ |

## نحوه استفاده

بهم بگو کدوم شماره‌(ها) رو انجام بدم (مثلاً «RT-001 و RT-002 رو برو انجام بده»).
اگه چیزی این‌جا نیست ولی لازمشه، بگو تا اضافه‌اش کنم.

---

## فازبندی سریع

### MVP / MVP-lite

- `RT-001..RT-010`
- `RT-011..RT-018`
- `RT-024`
- `RT-029`

### Post-MVP / Phase 2+

- `RT-019..RT-020`
- `RT-021..RT-023`
- `RT-025..RT-028`
- `RT-030..RT-031`
- `RT-032..RT-033` — roadmap Phase 2 (Skills): Skill Engine Core + Auto-Skill Detection, ✅ انجام شد ۲۰۲۶-۰۷-۱۲
- `RT-034..RT-037` — تکمیل roadmap Phase 2: Activation Client + Marketplace install-proxy (Skills/Plugins) + Plugin Settings UI + Plugin SDK/CLI، ✅ انجام شد ۲۰۲۶-۰۷-۱۲
- `RT-038..RT-043` — roadmap Phase 3 (هفته ۲۵-۲۸): Organization/Workspace CRUD تکمیل + Invitation + تکمیل RBAC + تعمیم Approval Queue + بودجه Wallet، ✅ انجام شد ۲۰۲۶-۰۷-۱۳
- `RT-044..RT-048` — roadmap Phase 3 (هفته ۲۹-۳۲): Role Catalog + Agent Hierarchy/Team + KPI + Agent-to-Agent messaging + Workflow Engine v1 (پوشش‌دهنده‌ی Task delegation)، ✅ انجام شد ۲۰۲۶-۰۷-۱۳ — جزئیات در `DONE.md`
- `RT-049..RT-053` — باقی‌مانده‌ی roadmap Phase 2 (Skills): versioning/fork + validation module + permission-diff consent + analytics + publisher dashboard (MKT-020..022 در پلن Marketplace)، ✅ انجام شد ۲۰۲۶-۰۷-۱۴ — WASM Sandbox عمداً کنار موند
- `RT-054..RT-056` — باقی‌مانده‌ی roadmap Phase 3 (هفته ۲۷-۲۸ Governance): Audit Log export/retention/tamper-evidence + HITL عمومی‌شده، ✅ انجام شد ۲۰۲۶-۰۷-۱۴ — جزئیات در `DONE.md`
- `RT-057..RT-067` — roadmap Phase 4 (هفته ۳۳-۴۰ Ecosystem): Marketplace payment debit + Outcome Engine (KPI/BI Dashboard/Insights) + Smart Features (auto-report/anomaly/predictive/NL query) + Integration Hub (webhooks/triggers/import-export)، ✅ انجام شد ۲۰۲۶-۰۷-۱۴ — جزئیات در `DONE.md`
- `RT-068..RT-075` — roadmap Phase 5 (هفته ۴۱-۴۸ Enterprise): SSO سازمانی (OIDC+SAML) + K8s manifests + backup/restore + SLA monitoring + DB sharding سند + Swagger docs + tutorials، ✅ انجام شد ۲۰۲۶-۰۷-۱۵ — SOC2/VPC/multi-region/CDN/case-studies/sales-kit صراحتاً خارج scope، جزئیات در `DONE.md`

### وضعیت خاص

- `RT-005` هنوز منتظر bot token واقعی از طرف شماست.
- `RT-013` عملاً به Plane 3 منتقل شده و باید در memory دنبال شود.
- `RT-069` (SAML) کد کامل، منتظر یه IdP واقعی (Okta/Azure trial) برای تست end-to-end.

> فازها و انتظار سیستم: `docs/spect/09_TASKS/13-phase-expectations.md`

---

## فاز 3 تا 5 در Runtime

### Phase 3 — Organization

- `RT-021..RT-031`, `RT-038..RT-048`, `RT-054..RT-056`
- هفته ۲۵-۲۸ (Org/Workspace CRUD + Invitation + RBAC + Approval + Wallet) ✅ انجام شد ۲۰۲۶-۰۷-۱۳؛ هفته ۲۹-۳۲ (Digital Employee + Agent Teams) هم ✅ انجام شد ۲۰۲۶-۰۷-۱۳؛ هفته ۲۷-۲۸ Governance تکمیل (Audit Log + HITL عمومی) ✅ انجام شد ۲۰۲۶-۰۷-۱۴
- وابستگی‌های roadmap: `T-077..T-104`

### Phase 4 — Ecosystem

- runtime touchpoints: `RT-027`, `RT-030`, `RT-031`, `RT-057..RT-067`
- هفته ۳۳-۴۰ (Marketplace پیشرفته + Outcome Engine + Smart Features + Integration Hub) ✅ انجام شد ۲۰۲۶-۰۷-۱۴ — جزئیات در `DONE.md`
- وابستگی‌های roadmap: `T-105..T-132`

### Phase 5 — Enterprise

- runtime touchpoints: `RT-068..RT-075`
- هفته ۴۱-۴۸ (SSO + K8s/backup/SLA + سند sharding + Swagger/tutorials) ✅ انجام شد ۲۰۲۶-۰۷-۱۵ — SOC2/VPC/multi-region/CDN/case-studies/sales-kit خارج scope، جزئیات در `DONE.md`
- وابستگی‌های roadmap: `T-133..T-160`
